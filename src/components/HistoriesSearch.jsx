import { memo } from "react";

function HistoriesSearch() {
  return (
    <section className="absolute z-50 top-full left-0 right-20 p-4 rounded-md bg-white shadow">
      Histories search
    </section>
  );
}

export default memo(HistoriesSearch);
